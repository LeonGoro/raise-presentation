# СТАРТОВОЕ СООБЩЕНИЕ ДЛЯ НОВОЙ СЕССИИ (скопируй всё ниже и отправь первым сообщением)

---

Привет! Продолжаем проект RAISE. Ты подключаешься ПОСЛЕ большой проделанной работы,
твоя задача — аккуратно продолжить, НЕ переделывая сделанное.

СНАЧАЛА, до любых действий, прочитай два файла в подключённой папке:
1. raise-presentation/HANDOFF-CLAUDE.md — полный контекст, все правки и правила
2. CONTEXT.md (в корне leontrade/) — правила портов и проектов

ЖЁСТКИЕ ПРАВИЛА (нарушение любого = откат работы):
- Порт RAISE = 5174. Порт 5173 = ДРУГОЙ проект LeonTrade, НЕ ТРОГАТЬ НИКОГДА.
- НЕ переписывай файлы целиком. Только точечные правки (Edit), после КАЖДОЙ —
  проверка: `node node_modules/typescript/bin/tsc --noEmit -p tsconfig.json`
- НЕ делай `npm install` из своей Linux-песочницы — сломаешь нативные бинарники
  на моём Маке. Новые зависимости — только прошу поставить меня.
- Figma MCP НЕ вызывать (лимит тарифа исчерпан). Все ассеты уже выгружены локально:
  figma-v03/ (слайды), public/assets/ (кропы, логотипы).
- Дизайн-решения уже приняты (тёмный космос, пара Manrope/Inter, без плашек и теней,
  glow вместо теней, кино-скролл) — НЕ менять направление без моего запроса.
- git push НЕ делать без моего явного «пушь» (на Railway автодеплой).
- Мой стиль: русский язык; когда я говорю «накидываю правки» — молча собирай их
  списком в HANDOFF-CLAUDE.md и НЕ делай ничего, пока не скажу «готово/применяй».

ТЕКУЩЕЕ СОСТОЯНИЕ: версия v5 «Deep Space» собрана, tsc чист, прошла агента-критика.
НЕ проверена визуально — мой dev-сервер упал в конце прошлой сессии.

ТВОИ ПЕРВЫЕ ШАГИ (по порядку, без самодеятельности):
1. Прочитай HANDOFF-CLAUDE.md и CONTEXT.md.
2. Попроси меня запустить: cd raise-presentation && npm run dev
3. Открой http://localhost:5174 через Chrome MCP и пройди страницу по чекпойнтам
   скролла (шаг ~1500px, скриншоты). Ищи: фризы, кривую вёрстку, наложения текста,
   обрезанные элементы, нечитаемый текст.
4. Каждый найденный дефект: сначала покажи мне список, потом чини точечно.
5. Дальше работаем по списку «ОТКРЫТЫЕ TODO» из HANDOFF-CLAUDE.md.

ЧЕГО ОТ ТЕБЯ ЖДУ: аккуратность важнее скорости. Не выдумывай контент — все тексты
уже в App-Landing.tsx (дословно из утверждённого макета). Если сомневаешься — спроси.

---
